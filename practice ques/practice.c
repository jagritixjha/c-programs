#include <stdio.h>

int main()
{
  int n = 1900, m, o, d;

  // printf("enter number: ");
  // scanf("%d", &n);

  // if ((n % 4 == 0 && n % 100 != 0) || (n % 400 == 0))
  //     printf("leap year");
  // else
  //     printf("not a leap year");

  // printf("\nenter number: ");
  // scanf("%d", &m);
  // printf("\nenter number: ");
  // scanf("%d", &m);

  // d = m*m-4*n*m;
  // if(d>0)
  //     printf("eq is real and distinct");
  // else if(d ==0)
  //     printf("eq is real and equal");
  // else
  //     printf("eq is imaginary");

  // if ( n & 1 == 1)
  //     printf("n is odd");
  // else
  //     printf("n is even");

  // printf("%d", n - n % 10);

  // printf("%d", n * 10 + m);

  // printf("%f inr = %.2f usd",n,n/76.23);
  // 1 usd = 76.23 inr
  // x usd = 45 inr

  // n = n % 10 * 100 + n / 10;
  // printf("%d", n);
  // 123 = 312
}